<Window x:Class="WPFINotifyPropertyChanged.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFINotifyPropertyChanged"
        mc:Ignorable="d"
        Title="WPFNotifyPropertyChanged" Height="200" Width="380" WindowStartupLocation="CenterScreen">
    <DockPanel>
        <Button x:Name="ChangeButton" Content="Change" DockPanel.Dock="Top" Width="80" Click="ChangeButtonClick" Margin="10"/>
        <Grid Margin="10">
            <ListView ItemsSource="{Binding Items}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Колонка 1" DisplayMemberBinding="{Binding Column1}" />
                        <GridViewColumn Header="Колонка 2" DisplayMemberBinding="{Binding Column2}" Width="120" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </DockPanel>
    
</Window>
